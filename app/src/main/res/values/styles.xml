<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Style pour le texte de l'application -->
    <style name="AppText">
        <item name="android:textColor">@color/textColor</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <!-- Style pour les titres -->
    <style name="AppText.Title">
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Style pour le texte normal -->
    <style name="AppText.Normal">
        <item name="android:textSize">16sp</item>
    </style>

    <!-- Style pour le chatbot -->
    <style name="ChatbotText">
        <item name="android:textSize">16sp</item>
        <item name="android:paddingStart">12dp</item>
        <item name="android:paddingEnd">12dp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
    </style>

    <!-- Style pour les messages utilisateur -->
    <style name="ChatbotText.UserMessage">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:gravity">end</item>
    </style>

    <!-- Style pour les messages du bot -->
    <style name="ChatbotText.BotMessage">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:gravity">start</item>
    </style>
</resources>